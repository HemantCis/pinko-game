const pins = {
  startPins: 3,
  pinSize: 3,
  pinGap: 36
}

const ball = {
  ballSize: 7.5
}

const engine = {
  engineGravity: 2.5
}

const world = {
  width: 750,
  height: 720
}

export const config = {
  pins,
  ball,
  engine,
  world
}